# Grafana URL as seen from the Next.js process.
# Use http://localhost:3001 when running Next.js on the host (npm run dev).
# Use http://grafana:3000 when running Next.js inside Docker on the same network.
GRAFANA_INTERNAL_URL=http://localhost:3001
